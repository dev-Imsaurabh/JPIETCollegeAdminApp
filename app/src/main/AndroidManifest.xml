<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jpietcollegeadminapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/jp_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/jp_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.JPIETCollegeAdminApp">
        <activity android:name=".DeleteNotice.DeleteNoticeActivity"></activity>
        <activity android:name=".faculty.UpdateTeacherActivity" />
        <activity android:name=".faculty.AddTeacherActivity" />
        <activity android:name=".faculty.UpdateFaculty" />
        <activity android:name=".UploadPDF" />
        <activity android:name=".UploadImage" />
        <activity android:name=".DeleteNotice.UploadNotice" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>